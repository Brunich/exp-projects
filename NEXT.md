# Next Tasks

1. Add revised-quote email draft in `web-service-quotes` after extending an expired quote's validity.
2. Add webhook queue metrics endpoint in `api-leads-capture` for monitoring dashboards.
3. Add due-this-week follow-up count to `web-client-tracker` stats panel.
