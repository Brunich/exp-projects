# Next Tasks

1. Add quote revision history notes in `web-service-quotes` when validity is extended.
2. Add per-source breakdown on daily lead buckets for stacked charts in `api-leads-capture`.
3. Add due-this-week quick filter to `web-client-tracker` client table (click stat card or toolbar toggle).
