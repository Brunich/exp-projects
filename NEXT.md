# Next Tasks

1. Add client search and quick filters in `web-client-tracker` (name, company, overdue only).
2. Add GitHub Actions cron workflow for `api-leads-capture` dead-letter auto-purge.
3. Add quote follow-up email draft in `web-service-quotes` for expired sent quotes.
