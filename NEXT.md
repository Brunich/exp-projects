# Next Tasks

1. Add due-this-week follow-up count to `web-client-tracker` stats panel.
2. Add quote revision history notes in `web-service-quotes` when validity is extended.
3. Add per-source breakdown on daily lead buckets for stacked charts in `api-leads-capture`.
