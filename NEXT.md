# Next Tasks

1. Add scheduled reminder cron for `web-client-tracker` when SMTP is configured.
2. Add quote number and issue date fields to saved quotes in `web-service-quotes`.
3. Add dead-letter CSV export endpoint in `api-leads-capture`.
