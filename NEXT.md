# Next Tasks

1. Add bulk archive actions in `web-client-tracker` (web day).
2. Add dead-letter replay endpoint for `api-leads-capture` webhook queue (backend day).
3. Persist quotes to local storage in `web-service-quotes`.
