# Next Tasks

1. Add dead-letter CSV export endpoint in `api-leads-capture` (backend day).
2. Add Slack/webhook notifications for overdue follow-ups in `web-client-tracker`.
3. Add quote status field (draft/sent/accepted) in `web-service-quotes`.
