# Next Tasks

1. Add quote revision history notes in `web-service-quotes` when validity is extended.
2. Add follow-up urgency badges (today/tomorrow) to `web-client-tracker` table rows.
3. Add sample chart component in `api-leads-capture` docs consuming stacked `dailyBuckets.bySource`.
