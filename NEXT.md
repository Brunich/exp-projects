# Next Tasks

1. Add scheduled reminder cron for `web-client-tracker` when SMTP is configured.
2. Add optional lead upsert mode in `api-leads-capture` to refresh name/message on duplicate email.
3. Add quote number and issue date fields to saved quotes in `web-service-quotes`.
