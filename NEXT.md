# Next Tasks

1. Add scheduled reminder cron for `web-client-tracker` when SMTP is configured.
2. Add template search/filter on the custom templates page in `web-service-quotes`.
3. Add optional lead upsert mode in `api-leads-capture` to refresh name/message on duplicate email.
