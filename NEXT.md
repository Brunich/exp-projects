# Next Tasks

1. Add branded logo upload to PDF export in `web-service-quotes` (web day).
2. Add bulk replay for dead-letter webhook items in `api-leads-capture` (backend day).
3. Add custom service template editor in `web-service-quotes`.
