# Next Tasks

1. Sync webhook retry queue to Supabase in `api-leads-capture` for multi-instance workers.
2. Add quote validity extension quick action in `web-service-quotes` when following up on expired quotes.
3. Add `T` keyboard shortcut to open the activity timeline for the first visible client row.
