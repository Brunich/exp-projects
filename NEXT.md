# Next Tasks

1. Add CSV export for archived clients in `web-client-tracker` (web day).
2. Add webhook retry queue for failed deliveries in `api-leads-capture`.
3. Scaffold `web-service-quotes` (Next.js quote builder shell).
