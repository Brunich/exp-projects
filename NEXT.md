# Next Tasks

1. Add quote expiration reminders in `web-service-quotes` (flag quotes past valid-until date).
2. Add client search and quick filters in `web-client-tracker` (name, company, overdue only).
3. Add GitHub Actions cron workflow for `api-leads-capture` dead-letter auto-purge.
