# Next Tasks

1. Add branded logo upload to PDF export in `web-service-quotes` (web day).
2. Add custom service template editor in `web-service-quotes`.
3. Add filtered dead-letter replay by date or lead source in `api-leads-capture`.
