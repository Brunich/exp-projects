# Next Tasks

1. Persist quotes to local storage in `web-service-quotes` (web day).
2. Add PDF export for completed quotes in `web-service-quotes`.
3. Add bulk replay for dead-letter webhook items in `api-leads-capture`.
