# Next Tasks

1. Add scheduled reminder cron for `web-client-tracker` when SMTP is configured.
2. Add duplicate-template action in `web-service-quotes`.
3. Add lead deduplication by email in `api-leads-capture` POST /leads.
