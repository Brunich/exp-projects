# Next Tasks

1. Add `N` keyboard shortcut to open "Add client" from the clients dashboard.
2. Add quote validity extension quick action in `web-service-quotes` when following up on expired quotes.
3. Sync webhook retry queue to Supabase in `api-leads-capture` for multi-instance workers.
