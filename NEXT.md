# Next Tasks

1. Add dashboard stats panel in `web-client-tracker` (overdue count, pipeline breakdown).
2. Add revised-quote email draft in `web-service-quotes` after extending an expired quote's validity.
3. Add webhook queue metrics endpoint in `api-leads-capture` for monitoring dashboards.
