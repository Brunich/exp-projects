# Next Tasks

1. Add quote validity extension quick action in `web-service-quotes` when following up on expired quotes.
2. Add `T` keyboard shortcut to open the activity timeline for the first visible client row in `web-client-tracker`.
3. Add `GET /leads/stats` summary endpoint in `api-leads-capture`.
