# Next Tasks

1. Add client search and quick filters in `web-client-tracker` (name, company, overdue only).
2. Add quote follow-up email draft in `web-service-quotes` for expired sent quotes.
3. Optional Supabase sync for `api-leads-capture` multi-instance deploys.
