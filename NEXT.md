# Next Tasks

1. Add quote expiration reminders in `web-service-quotes` (flag quotes past valid-until date).
2. Add scheduled dead-letter purge job in `api-leads-capture` (auto-delete failures older than N days).
3. Add client search and quick filters in `web-client-tracker` (name, company, overdue only).
