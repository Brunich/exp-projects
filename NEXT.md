# Next Tasks

1. Add dead-letter replay endpoint for `api-leads-capture` webhook queue (backend day).
2. Persist quotes to local storage in `web-service-quotes` (web day).
3. Add PDF export for completed quotes in `web-service-quotes`.
