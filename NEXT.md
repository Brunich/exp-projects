# Next Tasks

1. Add client activity timeline in `web-client-tracker` (notes + reminder history per client).
2. Add quote expiration reminders in `web-service-quotes` (flag quotes past valid-until date).
3. Add scheduled dead-letter purge job in `api-leads-capture` (auto-delete failures older than N days).
