# Next Tasks

1. Add revised-quote email draft in `web-service-quotes` after extending an expired quote's validity.
2. Add due-this-week follow-up count to `web-client-tracker` stats panel.
3. Add daily lead count buckets to `GET /leads/stats` for charting dashboards.
