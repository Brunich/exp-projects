# Next Tasks

1. Add follow-up urgency badges (today/tomorrow) to `web-client-tracker` table rows.
2. Show revision count badge on saved quotes list in `web-service-quotes`.
3. Add weekly email digest endpoint for lead volume trends in `api-leads-capture`.
