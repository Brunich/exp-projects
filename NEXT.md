# Next Tasks

1. Add follow-up urgency badges (today/tomorrow) to `web-client-tracker` table rows.
2. Add sample chart component in `api-leads-capture` docs consuming stacked `dailyBuckets.bySource`.
3. Show revision count badge on saved quotes list in `web-service-quotes`.
