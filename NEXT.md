# Next Tasks

1. Add client delete/archive action in `web-client-tracker`.
2. Persist `api-leads-capture` leads to SQLite or file storage.
3. Add rate limiting and honeypot field validation on `POST /leads`.
