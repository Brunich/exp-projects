# Next Tasks

1. Add scheduled reminder cron for `web-client-tracker` when SMTP is configured.
2. Add lead deduplication by email in `api-leads-capture` POST /leads.
3. Add template search/filter on the custom templates page in `web-service-quotes`.
