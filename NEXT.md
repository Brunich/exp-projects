# Next Tasks

1. Add rate limiting and honeypot field validation on `POST /leads` in `api-leads-capture` (backend day).
2. Add CSV export for archived clients in `web-client-tracker`.
3. Add webhook retry queue for failed deliveries in `api-leads-capture`.
