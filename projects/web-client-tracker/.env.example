# Environment

# Public app URL (used for redirects in production)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Server-side client data file (created on first run with sample data)
CLIENTS_FILE=data/clients.json
